const SLUG='corrida-cmdpii-2026';const AAPI=`/api/eventos/${SLUG}/admin`;
async function adminApi(path,options={}){const r=await fetch(AAPI+path,{credentials:'include',cache:'no-store',headers:{...(options.body instanceof FormData?{}:{'Content-Type':'application/json'}),...(options.headers||{})},...options});const d=await r.json().catch(()=>({}));if(!r.ok){if(r.status===401){throw new Error('Faça login no Axoriin como administrador antes de abrir este CMS.')}throw new Error(d.mensagem||`Erro ${r.status}`)}return d}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function statusMsg(text,ok=true,state=''){const e=document.getElementById('saveState');if(!e)return;e.textContent=text;e.dataset.state=state||(ok?'saved':'error');e.style.color='';e.style.borderColor=''}
function setBusy(btn,busy,label){if(!btn)return;if(busy){btn.dataset.oldText=btn.textContent;btn.textContent=label||'Processando...';btn.classList.add('busy');btn.disabled=true}else{btn.textContent=btn.dataset.oldText||btn.textContent;btn.classList.remove('busy');btn.disabled=false}}
function markDirty(){statusMsg('Alterações não salvas',true,'dirty')}
function savedNow(text='Alterações salvas'){const t=new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});statusMsg(`${text} • ${t}`,true,'saved')}

function sidebar(active){return `<aside class="sidebar"><div class="brand">AXORIIN<small>EVENTOS</small></div><nav class="side-nav"><a class="${active==='page'?'active':''}" href="./">Página da corrida</a><a class="${active==='people'?'active':''}" href="participantes.html">Inscrições, pagamentos e kits</a><a class="${active==='results'?'active':''}" href="resultados.html">Resultados</a><a class="${active==='photos'?'active':''}" href="fotos.html">Fotos e conquistas</a><a class="${active==='metrics'?'active':''}" href="metricas.html">Métricas de acesso</a><a href="/eventos/corrida-cmdpii-2026/" target="_blank">Visualizar página pública</a><a href="/login.html">Login Axoriin</a></nav></aside>`}
